{
  "name": "mb-design-engineering-web",
  "version": "1.0.0",
  "private": true,
  "description": "Muslims In Construction event site — Box Hill Trek for READ Foundation, served as Cloudflare Workers static assets",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
