{
  "name": "mb-design-engineering-web",
  "version": "1.0.0",
  "private": true,
  "description": "MB Design Solutions — structural engineers, Birmingham. Static site on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
